<p style="color: green"><%= notice %></p>

<div class='container'>
  <div class='row'>
    <div class= "col-6">
    
      <%= render @post %>


      <div>
      <% if current_user.present? %>
          <%# <% if current_user.admin? || owner%> %>
          <%= link_to "Редактировать", edit_post_path(@post) %> |
          <%# <% end %> %>
          <%= link_to "К другим новостям", posts_path%>
          <br>
          <br>
          <%# <% if current_user.admin? || owner%> %>
          <%= button_to "Удалить", @post, method: :delete %>
          <%# <% end%> %>
        <%end%>
      </div>

    </div>
  </div>
</div>