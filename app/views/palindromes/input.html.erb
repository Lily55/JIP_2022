<h2>Введите n <= 10^6:</h2>

<%= form_with url: "/palindromes/view", method: :get do |form| %>
  <div>
  <%= form.label :n, "n:".upcase %>
  <%= form.number_field :num, in: 1..10**6, step: 1 %>
  <%= form.submit "Выполнить" %>

  <p><%= link_to "Выйти", logout_path%></p>
  </div>
<% end %>
